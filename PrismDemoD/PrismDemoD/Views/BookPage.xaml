<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="PrismDemoD.Views.BookPage"
             Title="Book Details">

  <StackLayout Padding="20">
    <Label Text="{Binding Book.Title}" FontSize="Large" LineBreakMode="WordWrap">
      <Label.IsVisible>
        <OnPlatform x:TypeArguments="x:Boolean" iOS="False" Android="False" WinPhone="True" />
      </Label.IsVisible>
    </Label>
    
    <Label Text="{Binding Book.Title}" FontSize="Medium" LineBreakMode="WordWrap" />
    <Label Text="{Binding Book.Year}" FontSize="Medium" LineBreakMode="WordWrap" />
    <StackLayout Orientation="Horizontal" Padding="0,16,0,0">
      <Label Text="Author: " FontSize="Medium" FontAttributes="Italic" />
      <Label Text="{Binding FirstName}" FontSize="Medium" />
      <Label Text="{Binding Author}" FontSize="Medium" />
    </StackLayout>
    <Label Text="{Binding Read}" />
  </StackLayout>
</ContentPage>